# CI Scripts

- ci-healthcheck.sh
  Performs a quick, non-interactive validation of the Expo project suitable for CI environments where native Gradle builds are not applicable.

Usage:
  CI=1 sh ./scripts/ci-healthcheck.sh
